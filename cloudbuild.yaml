steps:
  - name: gcr.io/cloud-builders/docker
    entrypoint: bash
    args:
      - '-c'
      - 'docker pull us-east1-docker.pkg.dev/$PROJECT_ID/websites/public:latest || exit 0'

  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - '-t'
      - 'us-east1-docker.pkg.dev/$PROJECT_ID/websites/public:latest'
      - '--cache-from'
      - 'us-east1-docker.pkg.dev/$PROJECT_ID/websites/public:latest'
      - .

images:
  - 'us-east1-docker.pkg.dev/$PROJECT_ID/websites/public:latest'

options:
  logging: CLOUD_LOGGING_ONLY